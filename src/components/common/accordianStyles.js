import { createStyles } from "@material-ui/core";

export const useStyles = (theme) =>
  createStyles({
    accordionSummary: {
      backgroundColor: "#ECF0F4",
      borderRadius: "20px",
    },
  });
